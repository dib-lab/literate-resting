'literate-testing' is a literate testing utility that extracts shell commands
from reStructuredText files.

See simple/ for an example; run 'make' in that directory.

See kp/ for instructions on using this to run through the khmer protocols.
